#include "main.h"

/**
 * print_rev - This program prints a string in reverse and then a new line.
 * @s: string
 * Return: Always 0.
 */

void print_rev(char *s)
{
	char i = 'z';

	while (i = 'z')
	{
		print_rev(i);
		i--;
	}
	print_rev('\n');
}
