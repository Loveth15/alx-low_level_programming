#include "main.h"
#include <stdio.h>

/**
 * _strlen - This program returns the length of a string
 *
 * @s: A string
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	size _strlen(const char *s);
}
