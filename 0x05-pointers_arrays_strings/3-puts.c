#include "main.h"
#include <stdio.h>

/**
 * _puts - A program that prints a string, followed by a new line.
 *
 * @str: string to be printed.
 *
 * Return: Always 0.
 */

void _puts(char *str)
{
	puts(str);

	puts("\n");
}
