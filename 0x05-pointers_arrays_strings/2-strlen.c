#include "main.h"
#include <stdio.h>

/**
 * _strlen - This program checks the length of a string
 *
 * @s: String
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	size_t _strlen(const char *s);
}
